# Ignore everything
**

# Allow Go module files
!go.mod
!go.sum

# Allow source code
!cmd/**
!internal/**

# Allow migrations
!migrations/**

# Allow config (but not local)
!config/*.yaml
!config/*.yml

# Allow scripts (entrypoint only)
!scripts/entrypoint.sh
